# Domain Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Move pure order domain logic into `libs/domain` as `@pmx/domain`.

**Architecture:** Add a package-based workspace project named `domain`. Move the existing CLOB order draft model and `toClobOrderDraft` pure function out of the API app and make `OrdersService` import it from `@pmx/domain`.

**Tech Stack:** npm workspaces, Nx package metadata, TypeScript, Vitest, Jest, NestJS.

---

### Task 1: Domain Workspace Boundary

**Files:**
- Modify: `tools/workspace/nx-workspace.test.mjs`
- Create: `libs/domain/package.json`
- Create: `libs/domain/tsconfig.json`

- [x] **Step 1: Write the failing workspace test**

Add a `domain` entry requiring `libs/domain/package.json`, `nx.name = "domain"`, tags `["type:lib", "scope:domain", "layer:domain"]`, and `build` / `test` targets.

- [x] **Step 2: Run test to verify it fails**

Run: `npm run test:nx-workspace`

Expected: FAIL because `libs/domain/package.json` does not exist.

- [x] **Step 3: Add minimal package scaffolding**

Create `@pmx/domain` with `build` and `test` scripts, CommonJS output for API runtime compatibility, and Nx metadata.

- [x] **Step 4: Run test to verify it passes**

Run: `npm run test:nx-workspace`

Expected: PASS.

### Task 2: Order Domain Migration

**Files:**
- Create: `libs/domain/src/orders/order-domain.test.ts`
- Create: `libs/domain/src/orders/order-domain.ts`
- Create: `libs/domain/src/index.ts`
- Modify: `apps/api/src/orders/orders.service.ts`
- Delete: `apps/api/src/orders/order-domain.ts`
- Delete: `apps/api/src/orders/order-domain.spec.ts`

- [x] **Step 1: Write the failing behavior test**

Add tests for `toClobOrderDraft` preserving the existing CLOB V2 draft behavior and unsupported tick-size rejection.

- [x] **Step 2: Run test to verify it fails**

Run: `npm run test --workspace @pmx/domain`

Expected: FAIL because `toClobOrderDraft` is not implemented/exported yet.

- [x] **Step 3: Move minimal implementation**

Move existing pure types and `toClobOrderDraft` into `libs/domain/src/orders/order-domain.ts`, export it from `libs/domain/src/index.ts`, and change `OrdersService` to import from `@pmx/domain`.

- [x] **Step 4: Run tests to verify pass**

Run: `npm run test --workspace @pmx/domain` and `npm run test --workspace @pmx/api -- order-domain orders.service`

Expected: PASS.

### Task 3: Workspace Integration and Docs

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json`
- Modify: `tsconfig.base.json`
- Modify: `apps/api/package.json`
- Modify: `docs/project-memory.md`
- Modify: `docs/module-index.md`
- Create: `docs/modules/domain.md`

- [x] **Step 1: Update workspace integration**

Add `@pmx/domain` to root build order, TypeScript paths, API dependency list, and npm lockfile.

- [x] **Step 2: Update docs**

Mark Domain migration as completed only after verification passes.

- [x] **Step 3: Verify full workspace**

Run: `npx nx show projects`, `npx nx run domain:build`, `npx nx affected -t build test`, `npm test`, `npm run build`, and `git diff --check`.

Expected: all commands exit 0.
